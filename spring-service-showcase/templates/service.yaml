apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-service-showcase.fullname" . }}
  labels:
    {{- include "spring-service-showcase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-service-showcase.selectorLabels" . | nindent 4 }}
