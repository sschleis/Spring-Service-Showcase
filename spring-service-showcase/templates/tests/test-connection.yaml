apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-service-showcase.fullname" . }}-test-connection"
  labels:
    {{- include "spring-service-showcase.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spring-service-showcase.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
